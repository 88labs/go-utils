# go-utils Agent Routing

@AGENTS.md
@.github/copilot-instructions.md

Use the existing per-module instructions and repository-local test-matrix gate as authoritative.
The guardrail is a workflow aid; it does not replace review or CI. Before production edits, use
focused tests and a green baseline; ask the user when a boundary or expected oracle is ambiguous.
