# Binaries for programs and plugins
*.exe
*.exe~
*.dll
*.so
*.dylib

# Test binary, build with `go test -c`
*.test

# codecov
coverage.txt
unit-tests.xml

# Output of the go coverage tool, specifically when used with LiteIDE
*.out

vendor/
# config/*.toml

.DS_Store
.idea/*

# Descrypted by sops
docker-compose.env.dec

# Decrypted by helm-secrets
*.dec

# GoLand(?)が作る一時的なバイナリ・ログ置き場
tmp
